services:
  reploy:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    networks:
      - supabase_default

networks:
  supabase_default:
    external: true
